#!/usr/bin/env python
from clize import run
from clippy_lib import get_recommendations


def main(path='.', num_recommendations=5):
    get_recommendations.recommend(path, num_recommendations)


if __name__ == '__main__':
    run(main)
